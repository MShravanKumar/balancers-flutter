import 'package:flutter/material.dart';

SizedBox widthSpaceBox({double? width}) => SizedBox(width: width ?? 10);
SizedBox heightSpaceBox({double? height}) => SizedBox(height: height ?? 10);
const double defaultIconSize = 18;
const BorderRadius defaultRadius = BorderRadius.all(Radius.circular(18.0));
const BorderRadius buttonRadius = BorderRadius.all(Radius.circular(8.0));
const BorderSide feildbordercolor =
    BorderSide(color: Color(0xffA7A7A7), width: 2);
const double borderWidth = 2.0;
const double screenHeightPercentage = 0.020;
